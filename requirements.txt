# Minimal dependency list for the curated paper-code release.
# This list supports code inspection and module import structure,
# but the repository intentionally does not provide a runnable end-to-end pipeline.

torch>=2.0
transformers>=4.37
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
datasets>=2.16
evaluate>=0.4
librosa>=0.10
accelerate>=0.26
setproctitle>=1.3
thop>=0.1.1